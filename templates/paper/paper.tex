% Created by `rpp new`. A stub: replace all of it. The only thing `rpp lint` needs is that
% this file exists; the document class and the build are yours (see build.sh in the README).
\documentclass{article}
\title{Working title for {{name}}}
\begin{document}
\maketitle
Write the paper here.
\end{document}
