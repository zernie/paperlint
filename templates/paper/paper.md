# Working title for {{name}}

Created by `paperlint new`. A stub: replace all of it. The only thing `paperlint lint` needs is that this file
exists. New papers are written in `paper.tex`, which `paperlint build` compiles.
