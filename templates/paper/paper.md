# Working title for {{name}}

Created by `rpp new`. A stub: replace all of it. The only thing `rpp lint` needs is that this file
exists. New papers are written in `paper.tex`, which `rpp build` compiles.
