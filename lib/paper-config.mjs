/**
 * Где потребитель объявляет свои настройки — ОДИН ИСТОЧНИК для всего, что имеет право
 * импортировать.
 *
 * 🔴 ПОЧЕМУ ЭТОТ МОДУЛЬ НЕ УБИРАЕТ ДУБЛИРОВАНИЕ ПОЛНОСТЬЮ, И ЭТО НЕ НЕДОДЕЛКА. Три хука
 * (`hooks/*.hook.mjs`) обязаны продолжать писать эти значения у себя: компилируемому хуку
 * запрещено импортировать что-либо, кроме `vigiles/hook`, и запрет этот содержательный —
 * способности хука равны его API-поверхности, поэтому разрешить импорты значит разрешить
 * дотянуться до `child_process`. Ограничение шире необходимого (импорт чистых констант
 * способностей не добавляет), и это разобрано в zernie/vigiles#245, но до послабления хуки
 * дублируют, а расхождение ловит сверка в `hooks/hooks.harness.mjs`.
 *
 * 🔴 А ВОТ ОСТАЛЬНЫЕ ДУБЛИРОВАЛИ БЕЗ ВСЯКОЙ ПРИЧИНЫ. `eslint-rules/papers.mjs` и
 * `skills/paper-pipeline/scripts/consumer.mjs` — не хуки, никакого ограничения на импорты у них
 * нет, и они могли брать значение отсюда всегда. Хуже того, их копии не были покрыты сверкой
 * вовсе: она сравнивала три хука между собой, а эти два жили рядом и могли разъехаться молча.
 * Найдено 2026-09-18 грепом уже ПОСЛЕ починки самой сверки — то есть чинилось написание
 * проверки, а её ОХВАТ никто не мерил.
 */

/** Ключ в `package.json` потребителя, из которого читаются настройки этого пакета. */
export const CONFIG_KEY = "research-paper-pipeline";

/** Умолчание: потребитель, не объявивший ничего, считается держащим статьи в `papers/`. */
export const DEFAULT_PAPERS_ROOT = "papers";
