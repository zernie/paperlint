# Python-зависимости пайплайна статьи. Ставятся ОДНОЙ командой, ручных шагов нет:
#
#   pip install -r node_modules/research-paper-pipeline/skills/render-paper/checkers-requirements.txt
#
# Инструменты, которым нужен не-Python (jar TeXtidote), ставит ensure-checkers.sh рядом —
# он же ПРОВЕРЯЕТ, что каждый чекер реально запускается, а не только «установлен».

jinja2>=3.0                  # {{macro}}-подстановка в paper_numbers.py

# 🔴 ПИН НЕСУЩИЙ, не косметика. aclpubcheck тянет bibtexparser; ветка 2.x — бета
# (2.0.0b9) с другим API, а sdist ветки 1.4.x не собирается под setuptools 68+.
# У 1.3.0 есть готовая СБОРКА (bibtexparser-1.3.0-py3-none-any.whl), поэтому
# `<2` разрешается в него и собирать нечего. Проверено 2026-09-04 в чистом venv:
# установка выходит в 0, `python -m aclpubcheck.formatchecker --help` запускается.
bibtexparser<2

# 🔴 aclpubcheck СТАВИТСЯ ОТДЕЛЬНО, С `--no-deps`, И ЕГО ЗДЕСЬ НЕТ НАМЕРЕННО.
# Замер 2026-09-17: он объявляет `tsv>=1.2,<2.0`, у которого НЕТ НИ ОДНОЙ ГОТОВОЙ СБОРКИ
# (`pip download --only-binary :all: tsv` → «from versions: none»), а его исходник не
# собирается современным setuptools:
#     AttributeError: install_layout. Did you mean: 'install_platlib'?
# Из-за одной этой строки падала установка ВСЕГО файла, и потому статья месяц уезжала
# без официальной проверки формата.
# Проверено, а не предположено: `tsv` в исходниках aclpubcheck не импортируется НИ РАЗУ
# (grep по `import tsv|from tsv` — 0 хитов), и без него
# `python3 -m aclpubcheck.formatchecker --help` печатает usage и выходит нулём.
# Поэтому ниже перечислены ЕГО СОБСТВЕННЫЕ зависимости, дословно из метаданных пакета,
# минус tsv. Список, а не `aclpubcheck` одной строкой, — чтобы следующая невыразимая
# зависимость читалась как отсутствующая строка, а не как непроверенное допущение.
# 🔴 ВЕРХНИЕ ГРАНИЦЫ СНЯТЫ, И ЭТО ЗАМЕР, А НЕ НЕБРЕЖНОСТЬ. Первая редакция этого списка
# копировала ограничения aclpubcheck дословно — и покраснела на CI, где Python новее:
#     ERROR: Could not find a version that satisfies the requirement pandas<2.0.0,>=1.5.0
#            (from versions: 2.1.1, 2.1.2, … 3.0.5)
# То есть верхняя граница пакета протухла: pandas 1.x под тот интерпретатор не издаётся вовсе.
# Проверено прогоном по НАСТОЯЩЕМУ PDF (aisec-2026/paper.pdf) на numpy 2.4.6 + pandas 3.0.5:
# чекер отработал и выдал 9 находок формата (A4, шрифт). Нижние границы оставлены — они
# отсекают заведомо древнее; верхних нет, потому что ни одна из них не проверена, а
# непроверенная граница ломает установку ровно тогда, когда окружение обновят.
# ⚠️ `--help` доказательством НЕ является: он печатает usage и выходит нулём даже когда
# pandas в этом же интерпретаторе не импортируется. Проверено 17.09, поймано на себе.
Unidecode>=1.3.6
pandas>=1.5.0
pdfplumber>=0.7.5
pybtex>=0.24.0
pylatexenc>=2.10
termcolor>=2.0.1
tqdm>=4.64.1

rebiber>=1.1.3               # зависимость aclpubcheck И наш собственный инструмент

# ⚠️ Вызывать ТОЛЬКО как `python3 -m aclpubcheck.formatchecker`. У пакета нет
# __main__.py, и форма `python3 -m aclpubcheck` падает с «cannot be directly
# executed» — именно так этот шаг однажды прожил всю жизнь не запускаясь.
