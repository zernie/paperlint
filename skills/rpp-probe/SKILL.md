---
name: rpp-probe
description: ЗАМЕР, НЕ РАБОЧИЙ СКИЛЛ. Существует ровно для одного вопроса — подтягивается ли плагин из ПРИВАТНОГО репозитория zernie/research-paper-pipeline. Если эта строка видна в листинге скиллов, значит канал работает и приватность не мешает. Удалить сразу после ответа (задача про Этап 3).
---

# rpp-probe — пробник канала дистрибуции

Этот файл ничего не делает. Он существует, чтобы у вопроса «ставится ли плагин из приватного
репозитория» был **наблюдаемый эффект**: без скилла подтянувшийся плагин и не подтянувшийся
выглядят одинаково, а это ровно тот класс отказов, за который в базе уже плачено трижды.

**Как читать результат.** Видно имя `rpp-probe` в листинге скиллов новой сессии — канал
работает на приватном репозитории. Не видно — не работает, и Этап 3 упирается либо в открытие
репозитория, либо в автоподхват скиллов подключённого репозитория внутри remote-сессии.

**Удалить**, как только ответ получен и записан в `05-plan-perenosa` в базе знаний.
