# skills/

Пока пусто — скиллы приезжают на Этапе 3 переноса.

Каталог существует, потому что на него указывает `.claude-plugin/plugin.json`
(`"skills": "./skills/"`), и манифест, указывающий в пустоту, — битый указатель.

Здесь лежал временный скилл-пробник `rpp-probe` (10.09.2026). Он был написан ради одного
замера — виден ли скилл приватного репозитория в листинге, — и удалён, как только замер
дал ответ. Ответ оказался про другое: канал плагина в remote-окружении не поднимается
вовсе, ни для приватного маркетплейса, ни для публичного (`installed_plugins.json` пуст,
каталога маркетплейсов нет). Полный разбор с прогонами — в приватных заметках автора
(`idei/paper-pipeline-extraction/05-plan-perenosa-2026-09-10.md`, блок «ЗАМЕР 10.09»).

<!--
  ⚠️ Здесь стояло ИМЯ приватного репозитория, снято 12.09.2026. Нашёл это собственный
  аудит `pre-public-audit` — но только после того, как внешнее ревью заставило его
  сканировать ПУТИ и имена, а не одно лишь содержимое файлов.

  Само по себе это не утечка данных: приватный репозиторий остаётся недоступным, как его
  ни называй. Но публичный файл не обязан подтверждать, что такой репозиторий существует
  и что в нём лежит документ по такому пути — это метаданные, которых читателю не давали.

  Правило одной строкой: в публичном репозитории ссылаемся на приватные заметки БЕЗ имени
  репозитория. Путь внутри оставляем — он полезен тому единственному, у кого доступ есть,
  и не говорит ничего остальным.
-->

