\section{Introduction}
% TODO: state the research question — this is a LaTeX COMMENT.
% The rule reads RAW text and today does not tell it apart from the paper, so here it STAYS
% SILENT. The hole is named in the rule's header and measured: on the live corpus there are
% zero such cases, so it is latent. The fixture keeps it visible — the first real case turns
% up and the assertion below has to be rewritten deliberately, not discovered after the fact.
Body text of the paper, in which no question is stated.
